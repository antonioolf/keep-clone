import styled from 'styled-components';

const sidebarWidth = 280;

const Container = styled.div`
    width: ${sidebarWidth}px;
    height: 100%;
    padding-top: 8px;
    //position: fixed;
    /* background-color: rebeccapurple; */
`;

export { Container, sidebarWidth };